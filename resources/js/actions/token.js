export const addToken = (token) =>{
    return {
        type: 'ADD_TOKEN',
        payload: token,
    }
}

export const addData = (token) =>{
    return {
        type: 'ADD_DATA',
        payload: token,
    }
}

export const addType = (token) =>{
    return {
        type: 'ADD_TYPE',
        payload: token,
    }
}
export const addComputer = (token) =>{
    return {
        type: 'ADD_COMPUTER',
        payload: token,
    }
}